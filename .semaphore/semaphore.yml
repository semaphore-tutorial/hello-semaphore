version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: f1-standard-2
    os_image: ubuntu2204
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - echo "$HELLO"
            - echo "$foo"
        - name: 'Job #2'
          commands:
            - echo "$HELLO"
            - echo "$foo"
      env_vars:
        - name: HELLO
          value: world
        - name: foo
          value: bar
